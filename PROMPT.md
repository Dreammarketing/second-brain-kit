# Точка входу

Це те, з чого починаєш, якщо ще нічого не робив.

**Що зробити:**

1. Відкрий Claude Code і створи **нову сесію**
2. Перетягни цей файл у вікно і напиши: **виконай цей файл**.
   Або скопіюй увесь текст нижче і встав одним повідомленням
3. Далі просто відповідай на питання і тисни «далі»

Яку папку вибрати при створенні сесії — не важливо. На кроці 5 Claude сам
переведе сесію туди, куди треба.

Якщо працюєш із термінала — запусти `claude` і встав текст нижче одним
повідомленням. Далі все однаково.

Більше нічого знати не треба. Claude веде сам і зупиняється після кожного кроку,
чекаючи твоєї відповіді.

Єдина вимога: Claude Code уже встановлений. Якщо ні — спочатку
https://github.com/Dreammarketing/claude-optimizer

---

# Інструкція для Claude

Далі все адресовано агенту. Людині читати не обовʼязково.

Ти — мій інженер по знаннях. Я не програміст.

Сьогодні ми збираємо мою особисту систему: базу знань в Obsidian, ізоляцію
контекстів між проєктами і памʼять асистента, яка не зникає між сесіями.

Усі інструкції лежать у готовому комплекті:
https://github.com/Dreammarketing/second-brain-kit

## Як ти себе поводиш весь час

1. Строго по кроках. Один крок — одне повідомлення. У кінці кожного питай
   «Ідемо далі?» і чекай відповіді. Не перестрибуй, навіть якщо здається швидше
2. Нічого не робиш мовчки. Спочатку пояснюєш людською мовою, що зараз станеться
   і навіщо, потім робиш
3. Жодного файлу не видаляєш і не перезаписуєш без мого «так»
4. Без жаргону. Неминучий термін пояснюєш одним рядком
5. Команда впала — поясни причину простими словами і запропонуй один наступний
   крок. Не перебирай варіанти навмання
6. Ключі я вставляю сам. Ти їх не питаєш і не показуєш у відповідях
7. У кінці кожного етапу одним рядком: що в мене тепер працює

## Крок 1. Розкажи, що ми робимо

Прочитай README комплекту за посиланням вище і перекажи мені своїми словами,
у пʼяти реченнях: що ми будуємо, навіщо і що я матиму в кінці.

Без переказу таблиць. Тільки суть.

Окремо поясни одним абзацом, чому там три різні памʼяті і чим вони
відрізняються — я маю це зрозуміти до того, як почнемо ставити.

Питай «Ідемо далі?»

## Крок 2. Моя копія комплекту

Комплект спільний для групи. Моя база знань — це мої клієнти, цифри
й домовленості, їм там не місце.

Тому спочатку я роблю **свою приватну копію**.

Дай мені точну інструкцію і дочекайся, поки я скажу, що зробив:

1. Відкрити https://github.com/Dreammarketing/second-brain-kit/generate
2. **Owner** — мій акаунт
3. **Repository name** — `my-brain`
4. **Visibility** — обовʼязково **Private**
5. Тиснути **Create repository**

Поясни мені двома реченнями різницю між копією з шаблону і форком,
і чому нам потрібна саме копія.

Коли я скажу, що готово — спитай адресу мого нового репозиторію.

Питай «Ідемо далі?»

## Крок 3. Забрати комплект на компʼютер

Поясни мені двома реченнями, навіщо ми фіксуємо одне місце для всього,
а не кладемо папки де прийдеться.

Структура, від якої не відступаємо:

```
~/ClaudeCodeProjects/            контейнер, тут живе все, що я роблю
~/ClaudeCodeProjects/my-brain/   моя база знань, вона ж сховище Obsidian
~/ClaudeCodeProjects/<проєкт>/   код кожного проєкту, поруч із базою
```

Зроби так:

1. Створи папку `~/ClaudeCodeProjects`, якщо її ще немає
2. Склонуй мій репозиторій саме в `~/ClaudeCodeProjects/my-brain`
3. Покажи список файлів, які там зʼявились

Якщо в тебе немає доступу до цієї папки — запроси його. Мені вискочить
підтвердження, попередь мене одним рядком, що зараз зʼявиться і що тиснути.

Свого варіанту шляху не пропонуй. Шлях один і він написаний вище.

Якщо папка `my-brain` уже існує і не порожня — нічого не перезаписуй.
Покажи, що в ній лежить, і спитай мене, що робити.

Питай «Ідемо далі?»

## Крок 4. Звести все в одне місце

Поки я вчився, папки могли розповзтись: щось у домівці, щось у Документах,
щось лишилось від попереднього воркшопу. Зараз зведемо все в контейнер,
нічого не втративши.

**Спочатку тільки дивись, нічого не чіпай.** Пройди домашню папку на два рівні
вкладеності і знайди папки з ознаками робочої: є `.git`, або `CLAUDE.md`,
або `.obsidian`, або `.claude`.

Покажи таблицею: повний шлях, на що це схоже, дата останньої зміни, розмір,
чи є git remote.

Потім покажи таблицю переносу — що звідки куди і що лишається на місці.
Розкладка така:

- база знань уже на місці, її не чіпаємо
- код проєктів і папки від попередніх воркшопів → `~/ClaudeCodeProjects/<коротка-назва>/`
- папки без ознак роботи не чіпаємо взагалі
- якщо знайдеш окреме сховище Obsidian з моїми нотатками — не переноси мовчки.
  Покажи його окремим рядком і спитай, що з ним робити

Дочекайся мого «так» на таблицю. Без «так» не переносиш нічого.

Переносиш так:

1. Перед першим переносом запиши всі поточні шляхи в `~/ClaudeCodeProjects/_MOVE-LOG.tsv`.
   Це моя можливість відкотити
2. Переноси по одній папці, не пачкою. Після кожної кажи, що саме перенеслось
3. Тільки `mv`. Ніякого `rm`, ніякого перезапису. Якщо в цільовому місці вже
   щось лежить — зупинись і спитай
4. Якщо в папці є `.git` — після переносу покажи `git status`, він має лишитись чистим
5. Порожні старі папки не видаляй. Покажи їх списком у кінці, я вирішу сам

Питай «Ідемо далі?»

## Крок 5. Перевести сесію в папку бази

Це найважливіший крок, поясни мені його простими словами.

Ти бачиш тільки ту папку, до якої привʼязана ця сесія. Прямо зараз це не база.
Якщо так лишити — половина роботи ляже повз базу, і замість однієї робочої
папки в мене знову стане три.

Якщо робоча папка сесії вже `~/ClaudeCodeProjects/my-brain` — скажи це
і переходь до кроку 6.

Якщо ні — переведи сесію в цю папку сам, інструментом зміни робочої директорії.
Мені вискочить запит на підтвердження. Попередь мене заздалегідь одним рядком,
що зараз зʼявиться і що тиснути.

Якщо такого інструмента в тебе немає — значить я в терміналі. Тоді дай мені
інструкцію: закрити сесію, виконати `cd ~/ClaudeCodeProjects/my-brain`,
запустити `claude` звідти, перетягнути цей файл ще раз і сказати
«продовжуємо з кроку 6».

Папка перемикається не миттєво, а коли крок завершиться. Тому не перевіряй
її прямо зараз, перевіриш на наступному кроці.

Питай «Ідемо далі?»

## Крок 6. Звідки починаємо

Спочатку покажи мені робочу папку цієї сесії. Вона має бути
`~/ClaudeCodeProjects/my-brain`. Якщо це не так — повернись на крок 5
і скажи мені, що пішло не так. Далі не йди.

У комплекті десять етапів, від `00-start.md` до `09-avtomatyka.md`.

Подивись, які з них у мене вже пройдені. Ознаки шукай на диску в папці бази
`~/ClaudeCodeProjects/my-brain`, а не питай у мене: чи є `CLAUDE.md` і `INDEX.md`,
чи є папка `projects/`, чи є скіл у `.claude/skills/brain/`, чи стоїть iai-pme,
чи піднятий Graphiti, чи є посилання `~/.claude/skills/brain` (ознака етапу 9).

Покажи таблицею: **етап — зроблено чи ні — за якою ознакою ти це визначив**.

Скажи, з якого етапу мені починати просто зараз.

Питай «Ідемо далі?»

## Крок 7. Працюємо

Відкрий файл того етапу, з якого починаємо, і веди мене строго за промптом,
що всередині нього.

Не роби нічого, чого немає в поточному етапі. Не забігай уперед, навіть
якщо бачиш, що далі знадобиться.

Одне правило діє поверх усіх етапів і не скасовується: все нове лягає
в `~/ClaudeCodeProjects/`. Код проєкту, агент, платформа, бот — окремою папкою
поруч із базою, ніколи не всередині неї. У базі на нього заводиться картка.

Пройшли етап — скажи одним абзацом, що я тепер маю, і спитай, чи йдемо
на наступний. Не переходь сам.
