# Етап 6 — iai-pme, памʼять асистента

## Що робимо і навіщо

База знань з етапу 3 — це те, що читаєш **ти**. Ти її свідомо пишеш
і свідомо правиш.

iai-pme — це інше. Це памʼять **асистента** про ваші розмови. Вона пишеться
сама, дослівно, без твоєї участі. Ти ніколи не кажеш «запамʼятай це»
і ніколи не питаєш «що ми вирішили минулого разу».

На початку кожної нової сесії двигун сам підкидає в контекст маленький
шматочок релевантної памʼяті. Асистент відкриває розмову вже знаючи,
хто ти і над чим ви працювали.

## Навіщо це потрібно

- не треба щоразу пояснювати контекст з нуля
- дослівно, а не переказом: зберігається те, що ти справді сказав
- все локально, зашифровано, без акаунтів і без API-ключів
- дешевше: витягнути з памʼяті приблизно на 88 відсотків дешевше,
  ніж змусити агента шукати по файлах

## Що буде без нього

Кожна нова сесія — чистий аркуш. Ти платиш токенами за те,
щоб щоразу вводити асистента в курс справи.

## Чим це не є

Це **не заміна** базі знань в Obsidian. Три системи співіснують:

| | Obsidian | iai-pme | Graphiti |
|---|---|---|---|
| Хто пише | ти руками | двигун сам | двигун сам |
| Що зберігає | свідомі нотатки | дослівні розмови | звʼязки і час |
| Правиться руками | так | ні | ні |
| Читаєш очима | постійно | майже ніколи | у дашборді |

## Чесно про обмеження

Двигун англомовний за задумом. Кирилицю захоплює і знаходить,
але релевантність просідає. Це не поломка, це відомий компроміс.

Установка збирає нативний рушій з Rust — це кілька хвилин компіляції
і близько 500 МБ на диску. Наберись терпіння.

## Найважливіше на цьому етапі

Ключ шифрування лежить локально. **Втратиш ключ — втратиш усю памʼять.**
Бекап робиться на кроці 5, не пропускай його.

## Що має бути до старту

- Пройдені етапи 0-5
- Node 18+, Python 3.11 або 3.12 — ставили на етапі 0
- ~500 МБ вільного місця

---

## ПРОМПТ — копіювати звідси до кінця файлу

Продовжуємо. Це етап 6 із девʼяти — локальний двигун памʼяті iai-pme.

Поводься так само: по кроках, з поясненнями, «Ідемо далі?» у кінці кожного.
Якщо команда впала — поясни причину простими словами і запропонуй
один наступний крок, не перебирай варіанти навмання.

## Крок 1. Що це і чи готовий пристрій

Спочатку поясни мені простими словами, у пʼяти реченнях, що таке iai-pme,
чим він відрізняється від моєї бази знань в Obsidian і чому вони не
замінюють один одного.

Далі перевір готовність і покажи таблицею:

- Python 3.11 або 3.12 — є, яка саме
- Node 18+ — є, яка версія
- Rust — є чи ні
- вільне місце на диску — скільки
- чи не стоїть iai-pme уже

Rust ще немає — так і скажи, поставимо на наступному кроці.

Питай «Ідемо далі?»

## Крок 2. Подивитись, що саме буде зроблено

Установка йде однією командою, яка викачує скрипт з інтернету і виконує його.
Це нормальна практика, але перед тим як щось виконувати з мережі,
на це треба подивитись.

Запусти установник у режимі перевірки, без жодних змін:

```
curl -fsSL https://raw.githubusercontent.com/CodeAbra/iai-personal-memory-engine/main/scripts/bootstrap.sh -o /tmp/iai-bootstrap.sh
bash /tmp/iai-bootstrap.sh --preflight-only
```

Покажи мені, що він пише про мої передумови.

Далі — сухий прогін, теж без змін:

```
bash /tmp/iai-bootstrap.sh --dry-run
```

Перекажи мені людською мовою списком, що саме він збирається зробити:
куди поставить, що змінить у моїх налаштуваннях, що запустить у фоні.

Спитай прямо: «Згоден це виконати?» і чекай мого «так».

## Крок 3. Установка

Виконай установку:

```
bash /tmp/iai-bootstrap.sh
```

Якщо бракує Rust — постав його спочатку через rustup і поясни одним рядком,
що це таке і навіщо тут.

Збірка йде кілька хвилин. Поки вона йде, розкажи мені, як двигун влаштований:
що таке захоплення розмови, що він робить з нею вночі і що таке
«пакет памʼяті» на початку сесії. Простими словами, без термінів.

Коли закінчиться — покажи, що встановлено і куди.

Питай «Ідемо далі?»

## Крок 4. Перевірка

Запусти повну самодіагностику:

```
iai-mcp doctor
```

Покажи мені результат. По кожній червоній позиції скажи, чи це справді
проблема, і як лагодити.

Окремо перевір:

- `iai-mcp daemon status` — двигун живий
- чи зареєстрований MCP-сервер у Claude Code: `claude mcp list`

Попередь мене про те, що поломкою **не є**: стан `DREAMING` годинами,
`deferred` у лозі захоплення, кілька секунд на холодному старті.
Щоб я не лякався.

Питай «Ідемо далі?»

## Крок 5. Бекап ключа — не пропускай

Це найважливіший крок етапу.

Знайди файл ключа шифрування і покажи мені його точний шлях.

Поясни мені двома реченнями, що станеться, якщо я його втрачу.

Далі проведи мене через бекап: скопіювати папку з даними в надійне місце
поза цим компʼютером. Запропонуй конкретний спосіб і дочекайся, поки
я скажу, що зробив.

Перевір, що ключ і дані **не** потрапляють у Git.

Питай «Ідемо далі?»

## Крок 6. Згодувати двигуну мою базу

Тепер навчимо двигун тому, що вже є в моїй базі знань.

Покажи мені команду, якою можна згодувати йому окремий файл, і поясни,
чим це відрізняється від автоматичного захоплення розмов.

Згодуй по черзі: `CLAUDE.md`, `INDEX.md`, картки моїх активних проєктів.

**Не годуй** особисті області з `areas/` без мого окремого дозволу —
спитай по кожній окремо.

Після цього перевір, що памʼять реально працює: постав двигуну два-три
запити про те, що ми з тобою робили сьогодні, і покажи, що він повертає.

Питай «Ідемо далі?»

## Крок 7. Дашборд і підсумок

Покажи мені, як відкрити дашборд памʼяті, і пройдись по ньому: що я там бачу,
що означає граф, де дивитись скільки токенів зекономлено.

У кінці скажи одним абзацом:

- що тепер відбувається автоматично, без моєї участі
- що я маю робити руками і як часто
- де лежить бекап ключа
- яка одна команда лагодить більшість проблем
